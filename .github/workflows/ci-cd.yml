name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Clear Node.js cache
        run: npm cache clean --force

      - name: Checkout repository
        uses: actions/checkout@v4  # Update to v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Deploy to Zoho Catalyst
        run: catalyst deploy

      - name: Print Node.js version
        run: node --version

